# v3 行路叙事层 · 样板设计稿（评审版 v2.1）

> **实读校验码：`NX-V3JRN-8Q2K4`**
> 状态：设计稿，零代码。按设计闸门纪律：本文档 → 外部评审 → 用户拍板 → 逐项授权后才动代码。
> 作者：Qoder Agent（主笔）；评审：GPT Thinking·Extended（2026-09-02，Worked 3m9s）；拍板：郭老师。
> **v2 修订**：按 GPT Extended 评审 6 项裁定落实（§3 事件适配层 / §4 引用化+Effect 白名单+breakthrough 减法 / §5 缺窗与跨窗规则 / §6 痕迹降级为视觉闭环 / §7 era 错位修正 / §8 双轴分离），全录见 §12。
> **v2.1 增补**：郭老师拍板前新增**声音层**维度（§6.5），§9 资产缺口同步（零新视频立绘不变，新增 ~8 个短音频）。

---

## 0. 一句话定位

在两次 `enter()` 之间插入「正历」叙事层：随境界 Token 进度推进，把当前境界的编年史素材从"点开才看的卷轴"变成"主动渗进界面的行路体验"。**Coding 事件只触发回响，不改写故事。**

承接 PROJECT-BRIEF 的产品本质（用户即王林、Token=人生刻度、27 境界节点）与 GPT 第二轮评审的 v3 框架（未历/正历/已历、四进度窗、"Token 控制命运，Coding 控制天气"）。

**四层边界（评审定调，全部 § 遵守）**：
Canon 讲"发生过什么"（chronicle.json）→ Journey 讲"何时显影"（journey 调度）→ Coding Event 讲"此刻天气"（标准事件回响）→ 用户状态记录"我已经历过什么"（运行时存档）。四者永不混写。

## 1. 样板选择：元婴期（完整 journey）+ 化神期（轻 journey）

| 候选 | 结论 |
|---|---|
| **元婴期 04_yuanying（1e6→1e7）** | ✅ 主样板。资产最全（官方 CG 背景 yuanyingCG、era_04 立绘）；故事最丰富且自带四幕结构（分身入世→云天宗炼丹→屠藤复仇→化凡悟道）；chronicle 条目完整（4 事件/5 敌/5 语录/1 名场面/8 彩蛋） |
| **化神期 05_huashen（1e7→1e8）** | ✅ 轻样板。紧随元婴破境之后，专门验证「Aftermath 余韵 → 新卷展开」衔接；只做 entry+threshold 两窗（轻颗粒度 2 Beat） |
| 问鼎期+阴虚阳实 | 备选否决：问鼎背景复用结丹资产，视觉验证要先补资产，违背"样板先验证机制"原则 |

**验证闭环（GPT 定义的 9 步，本样板全走通才算过）**：
进入时代 → 30% 世界变化 → 错误触发敌人回响 → 工具调用触发法宝回响 → 65% 故事显影 → 90% 临界异象 → 100% 现有破境 → 余韵 → 新卷展开。

## 2. 现状挂点（v2.8 已有，全部复用不动）

| 现状 | 位置 | v3 用法 |
|---|---|---|
| Token 真源 | `quota.json` → `/api/nitian/usage` → `poll()` 30s 轮询 | 进度百分比 `(total-from)/(to-from)` 在 paint(idx) 时计算 |
| 境界判定 | `realmOf(total())`，跨节点 `enter(ni)` | 窗口跃迁检测：pct 过 30/65/90 阈值时触发窗渲染 |
| 报错事件 | MutationObserver → `doom()`（9s 节流）→ 敌人立绘+语录气泡 3.5s | 作为事件探针之一接入 §3 事件适配层 |
| 破境演出 | `enter(i)` 全屏视频（大 9s / 小 2.8s）+ 粒子 + 长按蓄力 | **完全不动**；只在其前后加临界异象与余韵 |
| 编年史 | `chronicle.json` → 卷轴 `openChron` 渲染 | 作为 journey 素材唯一来源；卷轴 UI 保留（"点开看全"） |
| 立绘/背景 | `CH.*` 时代立绘、`BG.*` 背景、电影海报 `V.*` | 四窗世界变化的素材 |

## 3. 事件源：案 A 先行 + 事件适配层（评审重点①，已裁定）

v2.8 只监听了"报错"。工具调用 / 长推理 / 任务完成三个事件源缺失。

- **案 A（纯前端 observer）**：扩展现有 MutationObserver，按节点 class/文本特征识别工具调用块、thinking 态、任务完成徽记。零后端改动，当天可上；识别精度受 DSH 前端 DOM 结构约束。
- **案 B（server 事件流）**：`pkg/src/index.ts` 增加事件 hook → `/api/nitian/event` 落盘 → 客户端随 poll 顺带读取。事件可靠、语义精确；但要动 server 侧代码（单独授权）且依赖 Harness 是否暴露生命周期钩子（未知数）。

**裁定：A-first 通过，但 A 与 Journey 系统禁止直接耦合。** 必须先定一个薄事件适配层：

```
DOM Observer（A）──┐
                  ├→ 标准事件 → Journey 回响系统
Server Hook（B）──┘
```

**标准事件契约（Event Adapter Contract，S3 前冻结）**：
- 事件类型：`error / tool / thinking / task_complete`
- 事件载荷：`{ ts 发生时间, fingerprint 指纹, sourceKey 探针标识, realmId 当前境界节点 }`
- 未来 A 换 B，只换"探针"，不重写 doom、法宝回响、打坐态和痕迹系统。

**升级条件（替代原"跑两周"）**：样板实测后，按关键事件的误报/漏报是否明显决定是否升级 B。单人项目不写死时间。

**A 案口径收紧（防误报）**：
- `thinking`（打坐态）= **AI 回合已开始、但持续无有效输出**，不是用户页面静置 45s（正常看代码不得误判为闭关）。
- `task_complete` 只认明确的 DSH 完成态/徽记；DOM 没有可靠完成信号时**宁可不触发，也不从普通文本里猜"完成/done"**。

## 4. 数据结构：chronicle.json 扩展（引用化 + Effect 白名单）

现有键（events/enemies/treasures/friends/scene/eggs/quotes/era）回答"仙逆故事是什么"，保持不动。新增两节回答"怎么把故事讲给正在 coding 的用户"：

**Canon 稳定 ID（S1 第一步，现在是最便宜的补 ID 时点）**：S1 时给 04/05 两个阶段的 events/enemies/treasures/friends/quotes 条目补稳定 `id`（如 `en_04_01`、`qu_04_03`、`tr_04_shalu`）；24 阶段铺量时同规则跟进。**journey 只存引用，不复制 Canon 内容**——名称、语录、来源改错字时 journey 永不产生第二份旧数据。

**Effect 白名单（防 if/else 搬进 JSON）**：vfx 只允许以下有限可复用类型，每个 Beat 只声明 `type + asset 引用 + 强度`；新类型需设计评审进白名单，**禁止境界级随手发明新字段**（fog_density / moon_crack 之类一律拒绝）：

| Effect 类型 | 语义 |
|---|---|
| `bg_swap` | 背景替换/渐显 |
| `char_in` / `char_out` | 角色/剪影出现或退场（`temp:true` 表示叙事临时展示，不改用户角色选择） |
| `ui_intensity` | 界面强弱变化（海报压暗、卷轴边角点亮） |
| `particle_level` | 粒子强弱 |
| `seal_pulse` | 境界印/徽记呼吸 |
| `crack_glow` | 纹路/裂纹点亮 |

```jsonc
// 04_yuanying 增量（示意，评审重点②，v2 引用化后）
"journey": {
  "entry":    { "era_line": "罗天星域 · 纵横一方",
                "hint": "分身入世，云天宗炼丹攒家底",
                "fx": [ { "type": "bg_swap", "asset": "BG.yuanyingCG", "fade": 1.2 },
                        { "type": "char_in", "asset": "CH.era_04", "temp": true } ] },
  "rising":   { "hint": "云天宗夺权 · 与李慕婉重逢",
                "scroll_refs": ["ev_04_yuntianzong", "fr_04_limuwan", "tr_04_shalu"],
                "fx": [ { "type": "ui_intensity", "asset": "scroll_edge", "lv": 1 } ] },
  "crisis":   { "hint": "四百年血仇 · 藤家在列",
                "enemy_refs": ["en_04_tenghuayuan", "en_04_tengjialaozu", "en_04_tengyuanying"],
                "quote_refs": ["qu_04_xing_teng", "qu_04_sichou"],
                "fx": [ { "type": "ui_intensity", "asset": "poster_dim", "lv": 1 },
                        { "type": "char_in", "asset": "sil_tengjia", "temp": true } ] },
  "threshold":{ "hint": "化凡悟生死，临界只差一步",
                "fx": [ { "type": "seal_pulse", "lv": 2 },
                        { "type": "crack_glow", "lv": 1 } ] }
},
"breakthrough": {
  // v2 减法：样板期只定义 aftermath（破境后新增责任）
  // omen/verdict 为未来统一重构破境叙事时的扩展位，现在不实现、不造第二事实源
  "aftermath":{ "poster": "era_05 首展", "seal": "元婴卷盖「已历」印", "next_leak": "化神卷首条线索：神游太虚" }
}
```

**职责划分（消重叠）**：`journey.threshold` = 破境前全部责任；`enter()` = 破境高潮全部责任（原样）；`breakthrough.aftermath` = v3 新增的破境后全部责任。

**缺失窗口规则**：journey 未定义的窗口（如化神只有 entry/threshold）= **不产生新 Beat，但此前世界状态继续保持**（entry 的背景/立绘一直留到 threshold）。Journey 是"累积世界状态"，不是四张互相替换的幻灯片。

**原则**：journey 数据全部通过 refs 派生引用，不新写剧情事实；AI 扩展语录（`src: "GPT-Extend"` 的 3 条）进 crisis.quote_refs 前须在飞书知识库走核验流程（呼应"未经核验的 AI 候选不得成为 Canon 事实源"）。

## 5. 四进度窗（元婴期具体化）

窗口只由 **当前境界 Token 进度 pct** 驱动（内部导演参数，不展示给用户）：

| 窗口 | pct | 世界变化（永久感） | 用户感知 |
|---|---|---|---|
| 入世 | 0–30% | yuanyingCG 背景渐显替代通用底；era_04 分身立绘入场；时代名浮现 | "到了新地方" |
| 因起 | 30–65% | 卷轴边角点亮人物/法宝标签；远处云天宗剪影 | "这卷故事在展开" |
| 劫至 | 65–90% | 敌人池切换为藤家系；海报压暗频率上升；语录偏移到复仇线 | "要变天了" |
| 临界 | 90–100% | 常驻克制异象：境界印呼吸加快、仙玉环微震、裂纹微光 | "马上破境"但**不弹窗、不遮代码、可正常干活** |

红线：修为只升不降 ⇒ 世界只向前。窗口跃迁不可逆，离线多天后恢复 = 直接落在当前 pct 对应窗口，不重放。

**状态与演出分离（评审新增）**：
- 世界状态永远可由 `realmId + pct` 直接重建；一次性演出（30% 闪一下、65% 标签点亮）只响应**活跃 Session 内的实时跨窗**。
- 一次 poll 跨多个阈值（如 29%→67%，或关机三天 20%→92%）：**直接落最终世界状态，最多播放最新一次轻转场，不连续补放三段动画**——杜绝"回来打开插件被昨天错过的三套特效轮番轰炸"。

**角色偏好隔离（评审新增）**：Journey 可临时展示叙事角色/剪影（`temp:true`），但**不能改写用户的本尊/分身选择**。"世界导演状态"与"用户角色偏好状态"隔离，二者各存各的。

## 6. 即时回响表（Coding 事件 → 天气）

| 事件 | 现状 | v3 回响（窗口感知） |
|---|---|---|
| 报错 | doom()：固定敌人+语录 3.5s | 敌人池按窗口加权：入世期普通敌人，劫至期藤家剪影优先 + 语录偏移；保持 9s 节流 |
| 连续 3 次同源报错 | 无 | **痕迹系统 v3.0（视觉闭环，见下）** |
| 长推理 | 无 | 打坐态：AI 回合已开始但持续无有效输出 → 灵气流速降低、闭关徽记；用户操作即醒。非弹窗 |
| 工具调用 | 无 | 法宝虚影 1.2s：当前窗口已解锁法宝（元婴期 refs=杀戮珠/天逆珠）短闪。**世界观映射，不声称原著因果** |
| 任务完成 | 无 | 收束一拍：对应事件纹亮起 + 仙玉跳动 + 偶发"此劫已过。"（不每次大庆，防稀释破境高潮）。**只认明确完成态，无可靠信号不触发** |

**痕迹系统 v3.0（评审裁定：做视觉闭环，不做永久实名）**：

```
error fingerprint（标准事件提取，非显示文本比较）
→ 首次命中建立 trace，固定映射一个敌人剪影
→ 后续同源报错强化同一 trace（同一剪影累积出现）
→ 任务解决（该 fingerprint 消失/收到完成信号）→ 剪影碎裂 + 一次短暂揭名/提示
```

- "连续 3 次"阈值保留；"同源"定义 = 标准 `fingerprint`（如归一化报错类型+模块），**不是**比较显示文本（行号变化/不同任务同文本的歧义由此消解）。
- **v3.0 不改写编年史**：剪影碎裂后只有视觉层揭名一瞬，不落"？？？→永久实名"。"反复纠缠→最终解决→碎裂"的爽感先验证；永久解锁（Narrative Unlock State：跨刷新/跨 Session 的叙事解锁存档）模型定义清楚后 v3.1 再上——避免为一个 1.5s 的惊喜在样板里顺便造半套成就/存档系统。
- **chronicle.json 永远只读**：Canon 事实、导演配置、用户历程三种数据永不回写混入。

## 6.5 声音层（v2.1 新增，郭老师拍板前提出）

设计哲学：**不搞背景音乐，搞"世界在你耳边呼吸"**——写代码时循环旋律是大干扰，修真感靠环境音+稀疏音效撑，天花板是"注意到时才存在"。

| 层 | 内容 | 频率 |
|---|---|---|
| 底噪 | 每境界一条极轻循环环境音（元婴=风+隐约钟；临界窗渐入心跳/低频共振） | 常驻，音量低到不注意就听不见 |
| 事件音 | 报错=冷鼓一声；工具=法宝嗡鸣；完成=清磬一响；打坐=流水呼吸；跨窗=风起转场 | 每次事件一响，0.5–1.5s |
| 破境 | 现有破境视频自带音轨直接用，不加层；Aftermath 接一声钟鸣收尾 | 极少，保住高潮稀释门槛 |

硬约束：
- **总开关+音量**挂 opts（与 journey 开关同盘可关），音量默认 ~20%
- **浏览器 autoplay 限制**：AudioContext 首次用户交互后才解锁；实现用 Web Audio API 原生（client.js 约 +80 行，不引库）
- **音效跟随窗口**：事件音可被 journey 窗口调制（劫至期报错音更低沉），与视觉回响同一事件源驱动，不加新探针

## 7. 破境三连接（enter 不动，前后加拍）

1. **破境前**：临界窗异象随 pct→100 渐强（克制、无 modal）
2. **enter(5)**：现有全屏演出原样
3. **Aftermath 余韵（新增，~6s，Esc 可跳）**：视频结束不清场 → **era_05 海报首次完整展开** + 时代名"神游太虚 · 生死意境" + 元婴卷盖"已历"印 + 化神卷只露第一条线索 → 淡出回到 coding
   （v2 修正：v1 §4 误写"era_04 首展"，Aftermath 语义是进入新境界后展示**新**海报，统一为 era_05。）

## 8. 实现拆分（拍板后的执行序，每步独立可验可回滚）

| 步 | 内容 | 动什么 |
|---|---|---|
| S1 | **双轴分离**：Journey 进度驱动身份 = 27-node `realmId`，24-stage `chronicleKey` 只是素材来源（经映射表取用，现有 `chronAlias` 为雏形；**代码层从第一天不假设 realmId===chronicleKey**，空劫 1↔4 场景样板期就按此架构）；chronicle 04/05 补稳定 id + journey/breakthrough 数据扩展 + pct 判定 + 四窗渲染 | 数据 + client.js 渲染段 |
| S2 | doom 窗口化（敌人池/语录偏移，refs 化） | client.js doom 段 |
| S3 | 事件适配层 + 案 A observer（error/tool/thinking/task_complete 标准事件） | client.js observer 段（契约先冻结再写码） |
| S4 | 临界异象 + 打坐态 + 法宝虚影 + 完成收束 + 声音层（§6.5：底噪/事件音/音频资产接入） | 新增 UI 层 + assets/audio |
| S5 | Aftermath 余韵 | enter() 后新增层 |
| S6 | 化神轻 journey（验证余韵衔接 + 缺窗保持规则） | 数据复用 S1 管线 |

## 9. 资产缺口

**零新视频、零新立绘**。全部由现有素材组合：yuanyingCG / era_04 / era_05 / 卷轴 / 粒子 / 语录。打坐态与法宝虚影用 CSS+现有粒子。**新增音频（v2.1）**：~8 个短音效（报错/工具/完成/打坐/跨窗/钟鸣，每个 0.5–1.5s、<100KB）+ 2 条境界底噪循环；渠道三选一：免费商用素材库（首选，最快）/ 从现有破境视频抽音轨 / AGNES 生成（不确定性大，备选）。落 `assets/audio/`（本地，不入公共仓）。新增代码工作只在 client.js（预计 ≤700 行，含声音层 ~80 行）+ chronicle.json 两键扩展（含 04/05 条目补 id）。

## 10. 验收标准

1. 用 demoTokens 推进实测走完 §1 九步闭环，截图留档
2. 正常挂机 2-3 天真实 coding，主观判据："我真的在走王林元婴期这段人生"
3. 干扰性检查：临界窗/回响全程不弹 modal、不抢焦点、不遮代码；opts.journey 开关默认开、可关
4. 回归：enter 破境、卷轴、双风格切换、本尊/分身全部不受影响；**本尊/分身选择不被 journey 改写**；测后 demoTokens 复位 3999700000000
5. **chronicle.json 全程零回写**（测试前后 diff 校验）；刷新浏览器后世界状态与 realmId+pct 重建一致

## 11. 给评审模型的三个问题（已于 2026-09-02 GPT Extended 裁定）

① §3 事件源 A/B 取舍是否同意"先 A 后 B"？——**裁定：A-first 通过，但须加 Event Adapter 契约层、口径收紧、条件升级**（已落 §3）
② §4 journey 数据结构是否需要增删字段？——**裁定：外层四窗保留；内层 refs 引用化 + Effect 白名单 + breakthrough 减法只留 aftermath**（已落 §4）
③ §6 痕迹系统首版做不做？——**裁定：v3.0 做视觉闭环（纠缠→碎裂→短暂揭名），永久实名留 v3.1；"同源"以 fingerprint 定义**（已落 §6）

## 12. 评审记录

- **2026-09-02 GPT Thinking·Extended**（vip-14 镜像，Worked 3m9s，校验码引用核实）：总体裁定"框架通过，暂缓授权实现，先做一次很小的设计修订"——元婴完整样板+化神轻样板选择成立、九步闭环成立、总原则（Token 驱动故事、Coding 只做回响）完全同意；4 个结构点 + 6 项修改清单全部落进本 v2（§3/§4/§5/§6/§7/§8 各处标注）。评审同时确认的最大规模化返工点 = 24↔27 双轴（已落 S1）。修改完成后评审表态："这六项修完，我会支持进入用户拍板。"
- **2026-09-02 v2.1 声音层增补**：郭老师拍板前提出"音乐怎么设计"，确认补 §6.5（三层：底噪/事件音/破境；Web Audio 原生 +80 行；总开关+默认 ~20% 音量）+ §9 音频资产（~8 短音效 + 2 底噪，落 assets/audio 本地）+ §8 归入 S4。此节为拍板前新增维度，待 GPT 下轮对话顺带过目。
